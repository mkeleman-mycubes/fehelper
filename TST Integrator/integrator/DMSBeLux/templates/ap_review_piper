{
    "pipes": [
        {
            "type" : "action",
            "name" : "ap_review",
            "script" : "answers = body.answers; processInstanceId = answers.taskprocessinstanceid.data.answer[0]; formVariables = body.formVariables; secureData = body.secureData; taskId = body.answers['taskid'].data.answer[0]; headers.taskId = taskId"
        }
    ]
}